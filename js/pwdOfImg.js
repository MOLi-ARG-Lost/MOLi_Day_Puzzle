var hint = ['Looking carefully to find the hint.', 'You should focus on what you are typing.',
    'Keep trying!', 'Keep taping!'];
function _getHint() {
    alert(hint[Math.floor(Math.random()*4)]);
}